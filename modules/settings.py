# It is intended that this is copied into the repository and 
# used when running the tests with the --settings flag.
from .settings import *
TEST_RUNNER = 'tango_with_django_project.runner.NucleusRunner'
